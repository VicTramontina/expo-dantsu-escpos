import {
    SafeAreaView,
    StyleSheet,
} from 'react-native';

export default function App() {
    return (
        <SafeAreaView>

        </SafeAreaView>
    );
}


const styles = StyleSheet.create({
});
